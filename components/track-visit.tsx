"use client";

// Frozen contract (specs/CONTRACT-ui.md): V1 stub (docs/11-implementation.md
// › contrats gelés). Lot C (events/funnel) replaces the body with a real
// `navigator.sendBeacon` call to `api/events` — the props stay the same.
export const TrackVisit: (props: { slug: string }) => null = () => null;
