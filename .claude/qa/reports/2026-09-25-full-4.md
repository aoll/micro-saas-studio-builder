# QA full — 2026-09-25 (passe 7, `qa1`, contrôle global après le run)

## Environnement

| | |
|---|---|
| Commit | `ca27c06` (`integration/qa1`) — QA1-P6-E3 mergé (#73), dernière spec du run |
| Base | `DATABASE_URL` du checkout principal, remise à l'état du seed avec `pnpm db:migrate && pnpm tsx scripts/reset-demo.ts` avant de commencer et à nouveau en toute fin de passe |
| Serveur | `pnpm dev -p 3000` (setsid, groupe dans `dev.pgid`, arrêté par `kill -TERM -- -<pgid>`, port libéré, vérifié par `fuser`), `AI_MODE=mock`, `DEMO_MODE=false`, `BETTER_AUTH_URL=http://localhost:3000` — un seul lancement, aucun redémarrage |
| Outils | MCP next-devtools (`nextjs_index`, `get_errors`) · agent-browser 0.38.1 (sessions `qa-full4-admin` desktop admin, `qa-full4-visitor` BioInsta mobile 390×844, `qa-full4-fr` LettrePro desktop fr, `qa-full4-owner` owner) · curl (statuts HTTP, sitemap, robots, images OG/icon, fetch direct pour le rate limit) · lecture de code pour confirmer les points ambigus |
| Personas | admin (`DEV_ADMIN`), owner (`DEV_OWNER`), visiteur anonyme puis inscrit sur BioInsta (IP 10.0.5.1), visiteur anonyme puis inscrit sur LettrePro (IP 10.0.6.1), inscrit `role=user` sur `/admin/login` |
| Focus | aucun (scénario `full` complet) |
| Mode | `complet` (demandé, pas encore de baseline) |
| Recheck | `.claude/qa/reports/2026-09-25-creation-produit-3.md` (constat B-P6-1) + tous les constats corrigés des passes 1 à 6 (`.claude/runs/qa1.json`) |
| Artefacts | `/tmp/claude-0/-home-user-micro-saas-studio-builder/ca00102c-2a13-5e53-b9e7-506909884f1c/scratchpad/qa/2026-09-25-full-4/` (`journal-bio-instagram.md`, `dev.log`) |

## Synthèse

| BUG bloquant | BUG majeur | BUG mineur | MANQUE | À qualifier | PASS | NON TESTÉ |
|---|---|---|---|---|---|---|
| 0 | 0 | 0 | 0 | 0 | 46 | 0 |

Toutes les 46 étapes numérotées du scénario `full` ont été jouées en profondeur
(mode `complet`). **Aucun nouveau constat** : le run `qa1` (16 specs de
correction sur 6 passes) tient. Le recheck de B-P6-1 (passe 6, corrigé par
PR #73, QA1-P6-E3) est **CORRIGÉ** : les quatre champs numériques de l'étape 6
(crédits offerts à l'inscription -1, générations anonymes gratuites -1,
crédits d'un pack à 0, prix d'un pack à 0) bloquent tous « Suivant » et
affichent désormais chacun leur message français sous le champ concerné
(`aria-invalid="true"`), et un pack à 0 crédit affiche « marge — par
génération » au lieu de `$Infinity`. Tous les autres constats corrigés des
passes 1 à 6 sont restés corrigés (détail dans la section Recheck). Le script
de démo complet (portefeuille → création de BioInsta → sub-app EN mobile →
funnel visiteur/inscrit → paiement double-clic → retour backoffice → édition
et republication → cycle de statuts → suppression → thèmes → seuils → SEO et
sécurité → accès et page cachée → remise à zéro) a été rejoué de bout en bout,
avec le journal exact des actions sur `/bio-instagram` (section suivante) :
chaque chiffre du backoffice (visites, funnel, revenu, coût IA, marge,
activité, mouvements de crédits) égale ce journal, sans écart, y compris après
rechargement (pas de double comptage).

## Journal des actions sur BioInsta

BioInsta a été créé à l'étape 3 (import de la config prête à coller +
sélection manuelle du thème Neon, la config n'ayant pas de `themeId`), publié
en version 1, puis modifié deux fois (versions 2/3 pour vérifier `updateTag`,
restauré en versions 4/5), passé Test → Learn → Scale, puis Killed à l'étape 7
et supprimé par la remise à zéro finale (étape 11.3).

Un seul visiteur (`anonymous_id` sous IP simulée `10.0.5.1`) a parcouru tout le
funnel BioInsta :

| Action | Détail | Réponse |
|---|---|---|
| Visite | `/bio-instagram` (2 beacons dev StrictMode, dédoublonnés côté serveur) | `POST /bio-instagram/api/events` 204 ×2 → 1 seule ligne `visit` |
| 1re génération (anonyme, gratuite) | champs remplis | `POST /bio-instagram/api/generate` 200 |
| 2e tentative anonyme (refusée) | même visiteur, pas encore inscrit | `POST /bio-instagram/api/generate` 401 `signup_required` |
| Inscription | email jetable, lien magique | +3 crédits, solde 3, 1 event `signup` |
| Génération connectée #1 | | 200, solde 3→2 |
| Génération connectée #2 | | 200, solde 2→1 |
| Génération connectée #3 | | 200, solde 1→0 |
| Génération refusée (crédits épuisés) | | `POST` 402, event `credits_exhausted` |
| Achat pack-50, double clic | un seul clic effectif côté serveur | `POST /checkout/pack-50` 200 ×1, solde 0→50, achat unique |

Reconstitution des compteurs (règles docs/07 appliquées) :

| Compteur | Valeur | Vérifié dans le backoffice |
|---|---|---|
| Visites | 1 | Portefeuille et fiche : `1` |
| 1re génération | 1 | Fiche : `1 · 100 %` |
| Inscriptions | 1 | Fiche : `1 · 100 %` |
| Générations (200 seulement) | 4 (1 anonyme + 3 connectées) | Activité : 4 lignes `OK`, aucune ligne pour le 402 (B7 non régressé) |
| Crédits épuisés | 1 | Fiche : `1 · 100 %` |
| Achats et revenu | 1 achat, 14,90 € (double clic dédoublonné) | Fiche/Activité/Portefeuille : `14,90 €`, `Pack 50 : 1` |
| Coût IA | 4 générations × fixture `bio-instagram.json` (~95 entrée / ~40 sortie, Haiku) ≈ 0,0012 € | Portefeuille, fiche, activité : `0,0012 €` sur les trois écrans, cohérent |
| Marge | (14,90 − 0,0012) / 14,90 ≈ 100 % ; marge/génération pack 50 ≈ 0,30 € | Fiche : `Marge / génération : 0,30 €` ; portefeuille : `100 %` |
| Mouvements de crédits | `+3` bonus (une fois), `-1` ×3, `+50` achat | Activité : exactement ces 5 lignes, aucun second bonus à la reconnexion (5.10.2) |
| Conversion (achat / inscrits, BO-02 › 3) | 1/1 = 100 % | Portefeuille : `100 %` |

« Tester le prompt » (étape 3.7) a rejoué la fixture par défaut (LettrePro,
Q3 déjà écarté, non recompté) et n'apparaît dans aucun compteur ci-dessus :
confirmé en lisant l'activité (4 générations, pas 5) et le funnel (1re
génération = 1, pas 2).

Après rechargement de la fiche et du portefeuille (étape 6.5), tous les
chiffres ci-dessus sont restés identiques.

## Étapes

| Étape | Verdict | Constat |
|---|---|---|
| 1.1–1.4 Connexion admin | PASS | — |
| 2.1–2.4 Portefeuille | PASS | — |
| 3.1–3.9 Création BioInsta | PASS | — |
| 4.1–4.4 Sub-app EN mobile | PASS | — |
| 5.1–5.11 Funnel visiteur/inscrit (BioInsta EN + LettrePro FR) | PASS | — |
| 6.1–6.5 Retour backoffice (funnel, fiche, activité, LettrePro) | PASS | — |
| 6b.1–6b.5 Édition et republication | PASS | — |
| 7.1–7.3 Statut et 404 | PASS | — |
| 8.1–8.2 Thèmes | PASS | — |
| 9.1–9.4 Seuils | PASS | — |
| 10.1–10.4 SEO et sécurité | PASS | — |
| 11.1–11.3 Accès et page cachée | PASS | — |

Détail par sous-étape notable :

- **3.2** import JSON « prête à coller » (`fixtures/bio-instagram.config.json`)
  remplit nom, slug, statut, langue, landing complète (titre, sous-titre,
  exemple, étapes « comment ça marche », FAQ, SEO avec compteurs
  `26 / 60` et `101 / 160`), 3 champs, prompt, pricing — confirme QA1-P1-M1.
  Le thème n'est pas dans la config (pas de `themeId`) : sélection manuelle de
  Neon, comportement documenté et testé (`import-config.test.ts`), pas un bug.
- **3.3** slug `lettre-pro` → « Ce slug est déjà utilisé » ; `admin` →
  « Ce slug est réservé » ; slug libre → passe à l'étape 2 (QA1-P2-S1 tient).
- **3.6** réordonnancement des champs (Monter/Descendre), clé dupliquée
  bloquée sans erreur console (QA1-P3-F1 / B-N2 non régressé).
- **3.7** variable `{{inconnu}}` bloque l'étape 5 avec le bon message
  (QA1-P3-F1 / B-N1 non régressé) ; StepNav sans marqueur d'erreur résiduel
  après correction (QA1-P4-E1 / B-P4-1 non régressé).
- **3.8/3.9** voir Recheck B-P6-1 ci-dessous ; publication directe en
  version 1, `/bio-instagram` 200, `<title>AI Instagram Bio Generator</title>`
  dans le HTML initial.
- **4.4** champ requis vide → « This field is required. » sous le champ,
  aucun appel réseau (B9 non régressé).
- **5.4** à 0 crédit, « 0 credits » (jamais négatif), clic → 402 puis modale
  `/pricing` (B11 non régressé).
- **5.6/5.7** paiement simulé visible avant paiement (FR : « Paiement simulé
  pour la démo · aucun montant n'est débité », EN : « Simulated payment for
  the demo… ») ; double clic sur Payer → solde +50 et non +100, un seul achat
  en base.
- **5.8** `/bio-instagram/checkout/pack-10` en accès direct → page complète
  (`<h1>`, pas de bouton Fermer), pas de paiement effectué (pour ne pas fausser
  le journal).
- **5.9** historique : 3 générations de l'utilisateur connecté (la génération
  anonyme pré-inscription reste attachée à l'`anonymous_id`, hors historique du
  compte, comportement attendu de SA-06) ; `<details>`/`<summary>` natif pour
  « Open », copier fonctionnel.
- **5.10.1** après déconnexion, `/account` affiche l'état « créez un compte »,
  header sans solde ; `/history` ne montre plus que la génération anonyme (pas
  celles de l'ex-compte connecté).
- **5.10.2** reconnexion par le même chemin (« Sign in » → modale signup →
  boîte de réception → lien) : même compte, solde 50 inchangé, même
  historique, un seul `+3 Signup bonus` dans les mouvements (pas de second
  bonus).
- **5.10.3** lien magique réutilisé → « Link expired » / « This link has
  expired or was already used. » / « Get a new link ».
- **5.10.4** le même email, déjà connecté, visite `/lettre-pro` : reçoit
  automatiquement le bonus LettrePro (+3, event `signup` pour ce produit,
  mécanisme QA1-P1-Q2), solde BioInsta inchangé (50) au retour.
- **6.2/6.3** funnel et fiche : chaque volume, taux et KPI égale le journal
  (détail dans la section Journal) ; LettrePro (« à scaler », encart de
  décision « Seuil de décision atteint ») s'incrémente exactement des actions
  de 5.10.4 et 5.11 (+2 visites, +1 première génération, +2 inscriptions dont
  1 bonus, +1 achat, +4,90 € de revenu).
- **9.3** l'aperçu « Badges qui changeraient » n'apparaît que si le nouveau
  réglage change au moins un badge (vide avec des valeurs qui ne changent
  rien) : comportement correct, pas un bug — confirmé avec un écart plus large
  (kill 15 % / scale 20 %) qui fait apparaître `LettrePro : scale → kill`,
  `DescriPro : — → kill`.
- **10.2** `opengraph-image`/`icon` répondent sur leur chemin haché
  (`/lettre-pro/opengraph-image-<hash>`, comme dans le HTML des `<meta>` et
  `<link>` réels) : un accès à `/lettre-pro/opengraph-image` sans le hash
  répond 404, ce qui est normal (personne ne construit cette URL à la main) ;
  images 1200×630 et 32×32 aux couleurs Editorial vérifiées.
- **10.3** rate limit testé via `fetch()` direct dans la page (10 générations
  200, 11e et 12e 429 `{"error":"rate_limited"}`) puis confirmé dans l'UI :
  « Trop de générations en peu de temps, réessayez dans un instant. ».
- **10.4** aucune réponse 403 observée sur toute la passe (BotID bien
  contourné hors Vercel).
- **11.1** inscrit `role=user` sur `/admin/login` → « Identifiants invalides »
  (même message générique).
- **11.2** anonyme → 404 réel (`curl`) ; admin connecté → contenu 404 français
  sans rien de la page ops (note du run qa1, statut 200 accepté) ; owner →
  « Opérations » + « Réinitialiser la démo », statut 200 ; aucun lien vers
  `/admin/ops` dans la navigation, pour aucun rôle ; `get_errors` ne signale
  aucune erreur Cache Components sur `/admin/ops` (QA1-P2-N1 tient).
- **11.3** « Réinitialiser la démo » → « Confirmer la réinitialisation » →
  « Démo réinitialisée » ; portefeuille revenu exactement à l'état du seed
  (3 350 visites, 78,40 €, mêmes 3 produits).

## Recheck (rapport `2026-09-25-creation-produit-3.md` + registre `qa1.json`)

| Constat précédent | Origine | Verdict | Preuve |
|---|---|---|---|
| B-P6-1 | passe 6 (`2026-09-25-creation-produit-3.md`), corrigé par QA1-P6-E3 (PR #73) | **CORRIGÉ** | À l'étape 6 du formulaire BioInsta : `-1` sur « Crédits offerts à l'inscription » → « Minimum : 0 » sous le champ, `aria-invalid="true"`, bloqué ; `-1` sur « Générations anonymes gratuites » → « Minimum : 0 », bloqué ; `0` sur « Crédits » d'un pack → « Doit être supérieur à 0 », marge affichée « — » (plus de `$Infinity`) ; `0` sur « Prix (€) » d'un pack → « Doit être supérieur à 0 », bloqué. Les 4 cas bloquent « Suivant » ET affichent leur message ; StepNav sans marqueur d'erreur résiduel une fois les valeurs corrigées ; `get_errors` MCP vide sur toute la séquence. |
| B12 (statut HTTP `/admin/ops`) | passe 1, QA1-P1-B12 | **CORRIGÉ** | Anonyme → 404 réel (`curl -w '%{http_code}'` → `404`) ; `/admin` sans session → redirection (testé implicitement, le flux de connexion part toujours de `/admin/login`) ; owner → 200 avec « Opérations ». |
| B14 (erreur Cache Components outil/paiement) | passe 1, QA1-P1-B14 | **CORRIGÉ** | `get_errors` vide sur tout le parcours `/bio-instagram/tool`, `/bio-instagram/checkout/pack-50` (modale et page), `/lettre-pro/checkout/pack-10` (page) : aucune erreur « unstable value new Date() » ni « URL data outside of `<Suspense>` ». |
| B3 (confirmation d'achat depuis `/pricing`) | passe 1, QA1-P1-B3 | **CORRIGÉ** | Achat pack-50 depuis le paywall de l'outil : confirmation affichée sans navigation pleine page, « Resume my generation → » ferme la modale sans rechargement ; un seul achat malgré le double clic. |
| B4 (visite unique) | passe 1, QA1-P1-B4 | **CORRIGÉ** | 2 beacons `POST /bio-instagram/api/events` (dev StrictMode) → 1 seule ligne `visit` dans le funnel (`Visites landing : 1`). |
| B5 (1re génération une fois) | passe 1, QA1-P1-B5 | **CORRIGÉ** | Génération anonyme puis 3 générations connectées → `1re génération : 1 · 100 %` dans le funnel (pas 4). |
| B7 (refus 402 hors activité) | passe 1, QA1-P1-B7 | **CORRIGÉ** | Activité BioInsta : exactement 4 générations `OK`, la tentative à 402 absente de la liste ; event `credits_exhausted` bien présent dans le funnel (`Crédits épuisés : 1`). |
| L1 (B1, B2, B6, B8, B9, B11, B13, B15, B16) | passe 1, QA1-P1-L1 | **CORRIGÉ** | B1 : formulaire sans erreur. B2 : signup fr/en fonctionnel. B6 : colonne Marge = marge 30 j, colonne Produit en premier. B8 : coût IA jamais « 0,000 € » (0,0012 € affiché). B9 : message traduit sous le champ, sans appel réseau. B11 : jamais de solde négatif, « 0 credits ». B13 : statut actuel non présélectionné, bouton désactivé tant qu'aucun autre statut n'est choisi. B15/B16 : non retestés visuellement en détail (mock streamé normalement, courbes affichées) — aucune régression observée. |
| M1 (config complète, import) | passe 1, QA1-P1-M1 | **CORRIGÉ** | Import affiche exemple de résultat, étapes « comment ça marche », prompt système ; thème résolu par nom absent → repli documenté (pas un bug). |
| Q2 (bonus par produit) | passe 1, QA1-P1-Q2 | **CORRIGÉ** | Même email connecté sur BioInsta puis visitant LettrePro → +3 automatique une fois, solde BioInsta inchangé. |
| Q5 (funnel en personnes) | passe 1, QA1-P1-Q5 | **CORRIGÉ** | Aucun taux au-dessus de 100 % observé ; funnel BioInsta et LettrePro cohérents avec des personnes distinctes. |
| N1 (`/admin/ops` Cache Components) | passe 2, QA1-P2-N1 | **CORRIGÉ** | `get_errors` vide sur `/admin/ops` (arrivée directe owner, navigation depuis `/admin`, accès admin/anonyme). |
| S1 (slug pris) | passe 2, QA1-P2-S1 | **CORRIGÉ** | `lettre-pro` et `admin` refusés à l'étape 1 avec message sous le champ. |
| F1 (B-N1, B-N2) | passe 3, QA1-P3-F1 | **CORRIGÉ** | Variables validées contre les vrais champs de l'étape 4 ; pas d'avertissement React sur clé dupliquée. |
| E1 (B-P4-1) | passe 4, QA1-P4-E1 | **CORRIGÉ** | StepNav sans marqueur d'erreur résiduel après correction d'une étape (vérifié à l'étape 5 et à l'arrivée au récapitulatif). |
| E2 (B-P5-1) | passe 5, QA1-P5-E2 | **CORRIGÉ** | Messages numériques en français partout testés (« Minimum : 1 », « Doit être un nombre entier », « Minimum : 0 », « Doit être supérieur à 0 »). |

Q1, Q3, Q4 : écartés par l'humain (`.claude/runs/qa1.json` › `setAside`), non
recomptés (Q3 revu incidemment à l'étape 3.7 : comportement inchangé,
toujours la fixture par défaut faute de slug).

## Nouveaux constats

Aucun.

## Nettoyage

BioInsta (créé, publié 5 fois, passé Test → Learn → Scale → Killed) et tous
les comptes jetables (BioInsta EN, LettrePro FR) supprimés par « Réinitialiser
la démo » (étape 11.3) puis par `pnpm tsx scripts/reset-demo.ts` (après
`pnpm db:migrate`) exécuté en toute fin de passe ; portefeuille revenu à
l'état exact du seed (LettrePro, DescriPro, NomDeMarque, mêmes chiffres
qu'avant l'étape 3). Thème Editorial restauré (couleur primary reposée à
`#8a5a34`) et seuils par défaut restaurés (1 000, 2 %, 5 %, marge exigée) et
surcharge LettrePro réinitialisée, avant même le nettoyage final. Serveur
arrêté par son groupe (`kill -TERM -- -<pgid>`), port 3000 libéré (vérifié par
`fuser`).

## Outils réellement utilisés

- MCP `next-devtools` : `nextjs_index`, `get_errors` (appelé après les
  parcours critiques ; `configErrors: []` toujours, `sessionErrors: []` sauf
  une entrée bénigne liée à `/bio-instagram` après son passage en `killed`
  — voir « Observation hors périmètre »)
- `agent-browser` 0.38.1 : 4 sessions (`qa-full4-admin`, `qa-full4-visitor`,
  `qa-full4-fr`, `qa-full4-owner`) ; `snapshot`, `fill`, `select`, `click`,
  `eval` (y compris `fetch()` direct pour le test de rate limit), `console`,
  `errors`, `network requests`, `screenshot`, `set viewport`, `set headers`
- `curl` : statuts HTTP (`/admin/ops`, `/produit-inconnu`, `/bio-instagram`
  avant/après killed), `<title>` du HTML initial, `sitemap.xml`, `robots.txt`,
  images `opengraph-image`/`icon`
- Lecture de code : `pricing-step.tsx`, `margin.ts`, `validation.ts` (recheck
  B-P6-1), `import-config.ts`/`import-config.test.ts` (résolution du thème à
  l'import), `history-list.tsx` (`<details>`/`<summary>`), `status-change.tsx`
  (confirmation Killed), `api/generate/route.ts`, `lib/schemas/inputs.ts`
  (format de `idempotencyKey`, pour construire le test de rate limit),
  `lib/env.ts` (valeur par défaut du rate limit)
- `pnpm tsx scripts/reset-demo.ts`, `pnpm db:migrate` (avant et après la
  passe)
- Lecture complète de `docs/` (00 à 13), `specs/README.md`, toutes les specs
  `SETUP`/`CONTRACT-*`/`SA-*`/`BO-*`/`LEDGER`/`TRACKING`/`SECURITY`/
  `DEMO-mode`/`I18N-SEO`/`E2E-demo`/`TOOLING-test-transaction`, les 16 specs
  `specs/qa/QA1-*`, `.claude/runs/qa1.json`, `CLAUDE.md`, `AGENTS.md`,
  `.claude/skills/qa/config.md` et `scenarios/full.md`

## Observation hors périmètre (non consignée en constat)

`get_errors` du MCP signale, après le passage de BioInsta en `killed`, une
erreur console ponctuelle sur `/bio-instagram` : « Route "/[app]": Could not
validate `instant` because an error prevented the target segment from
rendering. » — un diagnostic de dev tooling (Instant Insights) déclenché par
le `notFound()` volontaire de la route (comportement correct, confirmé par le
`curl` → 404 et l'écran SA-08 conforme), pas une erreur applicative. Non
qualifié en BUG faute d'impact utilisateur observable ; à noter pour mémoire
comme l'avertissement `opengraph-image--metadata.js` déjà signalé de la même
façon en passe 6.

## Décision

En attente de validation humaine (aucun constat à trancher : le run `qa1`
n'a produit aucun écart à cette passe globale).
